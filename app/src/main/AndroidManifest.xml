<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coffeebakeryadmin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".AccountActivity"></activity>
        <activity android:name=".ReportActivity" />
        <activity android:name=".DetailCustomerActivity" />
        <activity android:name=".ListCustomerActivity" />
        <activity android:name=".DetailReceiptActivity" />
        <activity android:name=".ListReceiptActivity" />
        <activity android:name=".AddPostActivity" />
        <activity android:name=".ListPostActivity" />
        <activity android:name=".DetailProductActivity" />
        <activity android:name=".AddProductActivity" />
        <activity android:name=".ListProductActivity" />
        <activity android:name=".AdminActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>